# Python dependencies
__pycache__/
*.py[cod]
venv/
.venv/
.env

# Node/React dependencies
node_modules/
build/
.pnp
.pnp.js

# Testing and production
coverage/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# System files
.DS_Store
# Local context bundle (not part of the project)
AI_Hairstyle_Recommender_context.txt

# Local-only markdown (prompts, notes, scratch docs), never published
*.local.md

# Credentials and secrets (never commit)
.env
*.env
kaggle.json
**/kaggle.json
.kaggle/
access_token
**/access_token

# Raw training images and downloaded archives (the features CSV is committed,
# the source images are not)
backend/data/raw/
**/face-shape-data/
*.zip
